# 차트 색이 검증된 라이트 팔레트 기준이므로 화면도 라이트로 고정한다.
[theme]
base = "light"
backgroundColor = "#f9f9f7"
secondaryBackgroundColor = "#fcfcfb"
textColor = "#0b0b0b"
font = "sans serif"

[browser]
gatherUsageStats = false

[server]
# 클라우드 서버에는 열어줄 브라우저가 없다. 로컬에서는 run.bat 이 대신 열어준다.
headless = true
